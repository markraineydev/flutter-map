class MapModel {
  String name, address;
  double latitude, longitude;

  MapModel({
    required this.name,
    required this.address,
    required this.latitude,
    required this.longitude,
  });
}
